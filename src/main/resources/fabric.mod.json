
{
    "schemaVersion": 1,
    "id": "${modid}",
    "version": "${mod_version}",
    "accessWidener": "${modid}.accesswidener",
	"license": "MIT",
	"environment": "*",
    "mixins": [
        "${modid}.mixins.json"
    ]
}